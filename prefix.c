#include<stdio.h>
#include<string.h>

int main()
{
    gets(str);
    while(str[i]!='\0')
    {
        if(str[i]==ALPHABET)
        {
            stack[++top]=str[i];
        }

        else{
            if(str[i]=="-")
            {
                stack[top-1]=stack[top-1]-
            }
        }
    }
}